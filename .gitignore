node_modules
dist
.env.local

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/
.gstack/

# Listado del equipo: mails personales, y este repo es publico.
*.local.sql
